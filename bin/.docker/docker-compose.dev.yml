version: '2'
# Dev config, mounts from host directory.

services:
  app:
    build: {}
    # If you'd like xdebug and other debug tools, and don't mind the slow down, swap to the following image:
    #image: ezsystems/php:7.0-v0-dev
    image: ezsystems/php:7.0-v0
    volumes:
     - ../../:/var/www
     - $COMPOSER_HOME:/home/ez/.composer
    depends_on:
     - db
    environment:
     - SYMFONY_ENV
     - SYMFONY_DEBUG
     - DATABASE_USER
     - DATABASE_PASSWORD
     - DATABASE_NAME
     - DATABASE_HOST=db
